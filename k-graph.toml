# K-graph resolution config.
#
# k-graph/ leaves declare abstract `data` (documents, media.scripts, media.videos).
# This file defines INFRASTRUCTURE sources and which sources each data tree can reach.
#
# Each source declares:
#   origin  — base domain or URL prefix (empty for local filesystem)
#   mapping — path | hash
#   pattern — how origin combines with path or hash (optional; sensible defaults)
#   mapper  — hash-map file (required when mapping = hash); path relative to repo root

[infrastructure.local]
mapping = "path"

# [infrastructure.s3bucket]
# origin = "https://media.cohesian.org"
# mapping = "path"
# pattern = "{path}"

[infrastructure.youtube]
origin = "https://www.youtube.com"
mapping = "hash"
pattern = "/watch?v={hash}"
mapper = "maps/youtube.toml"

# --- data trees: reachable sources ---------------------------------------------

[data.documents]
sources = ["local"]  # , "s3bucket"

[data.media.scripts]
sources = ["local"]

[data.media.videos]
sources = ["local", "youtube"]
serve = "youtube"
