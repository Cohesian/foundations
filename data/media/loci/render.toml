# Render paths — edit here when the output tree moves or is renamed.
#
# Paths are relative to this file's directory (data/media/loci/) unless absolute.
# render.py reads this on every run; CLI --dst-root / --src-root override for one-offs.

dst_root = "../videos"
src_root = "."
